<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1 class="mt-4 mb-2">All Chat</h1>
    <div class="container-xxl">
        <div class="row">
            <% for(let chat of chats){ %>
                <div class="col-3 p-2">
                    <div class="card text-center">
                        <div class="card-header">
                          <b>From:</b> <%=chat.from %>
                        </div>
                        <div class="card-body">
                          <h5 class="card-title">Message:</h5>
                          <p class="card-text"><%=chat.message %></p>
                        </div>
                        <div class="card-footer text-body-secondary">
                            <b>To:</b> <%=chat.to %>
                            
                            <div class="">
                                <b>Time : </b> <%=chat.created_at.toString().split(" ")[4] %> <br>
                                <b>Date : </b> <%=chat.created_at.toString().split(" ").slice(0,4).join(" ") %> <br>
                            </div>
                        </div>
                        <div class="d-flex justify-content-center gap-2 mb-2">
                            <div class="">
                                <form action="/chats/<%=chat._id %>/edit" method="get">
                                    <button type="submit" class="btn btn-secondary fs-6"><i class="fa-solid fa-pen-to-square"></i></button>
                                </form>
                            </div>
                            <!-- <div class="">
                                <form action="/chats/<%=chat._id %>/up" method="get">
                                    <button type="submit" class="btn btn-secondary fs-4"><i class="fa-solid fa-pen-to-square"></i></button>
                                </form>
                            </div> -->
                            <div class="">
                                <form action="/chats/<%=chat._id %>?_method=DELETE" method="post">
                                    <button type="submit" class="btn btn-danger fs-6"><i class="fa-solid fa-trash"></i></button>
                                </form>
                            </div>
                        </div>
                      </div>
                </div>
            <% } %>
        </div>
        <div class="position-fixed bottom-0 end-0 p-4">
            <form action="/chats/new" method="get">
                <button type="submit" class="btn btn-primary"><i class="fa-solid fa-square-plus fs-2"></i></button>
            </form>
        </div> <!-- ?_method=PATCH -->

    </div>
</body>
</html>