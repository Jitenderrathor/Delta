<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css
    ">
</head>

<body>
    <div class="container-xxl">
    <h2 class="mt-5 mb-3">Jujutsu Kaisen</h2>

        <div class="row row-cols-1 row-cols-md-2 g-4 mt-2">
            <%for(post of posts){ %>
                <div class="col">
                    <div class="card">
                      <div class="card-body">
                        <h5 class="card-title"> <%= post.username %></h5>
                        <p class="card-text"><%= post.content %></p>
                        <a href="http://localhost:8080/posts/<%= post.id %>" class="btn btn-primary">Show</a>
                        <a href="http://localhost:8080/posts/<%= post.id %>/edit" class="btn btn-secondary ms-3">Edit</a>
                        <form method="post" action="/posts/<%= post.id %>?_method=DELETE" >
                          <button class="btn btn-danger mt-3">Delete Post</button>
                        </form>
                      </div>
                    </div>
                  </div>
            <%} %>
        </div>
    </div>
    <div class="position-absolute bottom-0 end-0">
    <button type="button" class="btn btn-primary m-3 text-decoration-none"><a href="http://localhost:8080/posts/new" class="text-white">Create new</a></button>
</div>
</body>

</html>